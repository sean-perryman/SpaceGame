.container.well
  .col-md-6
    %h3 Listing Player Ships
    %table.table.table-striped  
      %tr
        %th Captain
        %th Ship Type
        %th Ship Name
        %th Fuel Total
        %th Fuel Remaining
        %th Cargo Spaces
        %th Fighters
        %th{:colspan => "3"}

      - @player_ships.each do |player_ship|
        %tr
          %td= player_ship.captain.name
          %td= player_ship.ship.name
          %td= player_ship.name
          %td= player_ship.fuel_total
          %td= player_ship.fuel_remaining
          %td= player_ship.cargo_spaces
          %td= player_ship.fighters
          %td= link_to 'Show', player_ship, :class => "btn btn-sm btn-default"
          %td= link_to 'Edit', edit_player_ship_path(player_ship), :class => "btn btn-sm btn-default"
          %td= link_to 'Destroy', player_ship, method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-sm btn-danger"
    
    %br/
    = link_to 'New Player Ship', new_player_ship_path, :class => "btn btn-default"
