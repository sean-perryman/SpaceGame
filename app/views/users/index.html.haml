Here will show a listing of all users with username being a link to show
that link will have a listing of all the associated captains

.container-fluid.well.col-md-8
	%table.table.table-striped
		%tr
			%th User Name
			%th Email Address
			%th Admin?

		- @users.each do |user|
			%tr
				%td= link_to user.username, user_path(user.id)
				%td= user.email
				%td= user.admin