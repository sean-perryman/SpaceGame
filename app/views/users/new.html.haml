%div{ :class => 'container well' }
	%div{ :class => 'col-md-6' }
		%h1 Create Account
			
		= simple_form_for User.new do |f|
			= f.input :username
			= f.input :email
			= f.input :password
			= f.input :password_confirmation

			= f.button :submit